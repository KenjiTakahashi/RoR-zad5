Years:
- @years.each do |y|
  = y.year+', '
%p
  - form_for([:admin, @year]) do |f|
    = f.label "Add new year:"
    = f.text_field :year
    = f.submit "Add"
%p
  = link_to "Back", root_path
%p
  = link_to "Sign out", destroy_user_session_path
